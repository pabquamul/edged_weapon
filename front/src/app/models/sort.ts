export class Sort {
  sorted: boolean;
  unsorted: boolean;
  empty: boolean;

  constructor(sorted, unsorted, empty) {
    this.sorted = sorted;
    this.unsorted = unsorted;
    this.empty = empty;
  }
}
